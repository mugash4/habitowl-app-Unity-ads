rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Helper function to check if user is authenticated
    function isSignedIn() {
      return request.auth != null;
    }
    
    // Helper function to check if user owns the document
    function isOwner(userId) {
      return request.auth.uid == userId;
    }
    
    // Helper function to check if user is admin
    function isAdmin() {
      return isSignedIn() && 
        get(/databases/$(database)/documents/admin_config/settings)
        .data.admin_emails.hasAny([request.auth.token.email]);
    }

    // ✅ NEW: User consents - users can only read/write their own
    match /user_consents/{document} {
      allow read: if isSignedIn() && resource.data.userId == request.auth.uid;
      allow write: if isSignedIn() && request.resource.data.userId == request.auth.uid;
    }
    
    // ✅ NEW: Crash reports - authenticated users can write, admins can read
    match /crash_reports/{document} {
      allow write: if isSignedIn();
      allow read: if isSignedIn(); // Admins check done in app
    }
    
    // ✅ NEW: Deletion requests - users can read/write their own
    match /deletion_requests/{document} {
      allow read: if isSignedIn() && resource.data.userId == request.auth.uid;
      allow write: if isSignedIn() && request.resource.data.userId == request.auth.uid;
    }
    
    // ✅ NEW: Data exports log - users can read their own
    match /data_exports/{document} {
      allow read: if isSignedIn() && resource.data.userId == request.auth.uid;
      allow write: if isSignedIn() && request.resource.data.userId == request.auth.uid;
    }
    
    // ✅ NEW: Deleted users archive - admin only
    match /deleted_users_archive/{document} {
      allow read, write: if isSignedIn(); // Admin check done in app
    }
    
    // ✅ NEW: Admin actions log - admin only
    match /admin_actions/{document} {
      allow read, write: if isSignedIn(); // Admin check done in app
    }

    // Existing rules - Users collection
    match /users/{userId} {
      allow read: if isSignedIn();
      allow create: if isSignedIn();
      allow update: if isSignedIn() && (isOwner(resource.data.uid) || request.resource.data.uid == request.auth.uid || isAdmin());
    }

    // Existing rules - Habits collection
    match /habits/{habitId} {
      allow read: if isSignedIn() && (isOwner(resource.data.userId) || isAdmin());
      allow create: if isSignedIn();
      allow update, delete: if isSignedIn() && (isOwner(resource.data.userId) || isAdmin());
    }

    // Existing rules - Admin config
    match /admin_config/settings {
      allow read: if isSignedIn();
      allow write: if isAdmin();
    }
    
    match /admin_config/api_keys {
      allow read: if isAdmin();
      allow write: if isAdmin();
    }

     // ✅ COMPLETE FIX: Promo offers with proper statistics tracking
    match /promo_offers/{offerId} {
      // Anyone authenticated can read active offers
      allow read: if isSignedIn();
      
      // ✅ CRITICAL: Allow incremental updates for statistics tracking
      // This allows impressions, clicks, conversions to increment atomically
      allow update: if isSignedIn() && (
        // Allow tracking metric updates
        request.resource.data.diff(resource.data).affectedKeys()
          .hasOnly(['impressions', 'clicks', 'conversions', 'lastImpressionAt', 'lastClickAt', 'lastConversionAt'])
        ||
        // Allow admin to update offer details
        isAdmin()
      );
      
      // System and admins can create new offers
      allow create: if isSignedIn();
      
      // Only admins can delete offers
      allow delete: if isAdmin();
    }
  
    // Existing rules - Analytics
    match /analytics/{analyticsId} {
      allow read: if isAdmin();
      allow create: if isSignedIn();
    }

    // Existing rules - Referrals
    match /referrals/{referralId} {
      allow read: if isSignedIn();
      allow create: if isSignedIn();
    }

    // Existing rules - Support Tickets
    match /support_tickets/{ticketId} {
      allow create: if isSignedIn();
      allow read: if isSignedIn() && 
        (resource.data.userId == request.auth.uid || isAdmin());
      allow update: if isAdmin();
    }

    // Deny all other requests by default
    match /{document=**} {
      allow read, write: if false;
    }
    
  }
}